# Graphiti MCP 환경 변수 설정
# 사용법: cp .env.example .env 후 값 수정

# ========================================
# Neo4j 설정
# ========================================
NEO4J_PASSWORD=password123

# ========================================
# OpenAI API
# ========================================
OPENAI_API_KEY=sk-your-openai-api-key-here

# ========================================
# LLM 모델 설정
# ========================================
# 메인 모델 (엔티티 추출, 관계 생성 등)
MODEL_NAME=gpt-5-mini

# 작은 작업용 모델 (분류 등)
SMALL_MODEL_NAME=gpt-5-mini

# LLM 온도 (0.0-2.0, 기본값: 0.3)
LLM_TEMPERATURE=0.3

# LLM 추론 수준 ('minimal', 'low', 'medium', 'high')
REASONING_EFFORT=minimal

# ========================================
# 성능 설정
# ========================================
# 동시 처리 제한 (LLM rate limit 방지)
# 기본값: 10
SEMAPHORE_LIMIT=10